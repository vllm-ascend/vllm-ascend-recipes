---
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

interface Props {
  modelId: string;
  compact?: boolean;
}

const { modelId, compact = false } = Astro.props;

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const verifyPath = path.join(__dirname, '..', '..', 'public', 'verify-status.json');

let status: string | null = null;
let runUrl: string | null = null;

try {
  if (fs.existsSync(verifyPath)) {
    const data = JSON.parse(fs.readFileSync(verifyPath, 'utf-8'));
    const model = data.models?.[modelId];
    if (model) {
      status = model.status;
      runUrl = model.run_url || null;
    }
  }
} catch {
  // silently skip if verify-status.json not found or invalid
}

if (!status) return null;

const colors: Record<string, string> = {
  PASS: 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20',
  FAIL: 'bg-red-500/10 text-red-400 border-red-500/20',
  SKIP: 'bg-amber-500/10 text-amber-400 border-amber-500/20',
};

const labels: Record<string, string> = {
  PASS: '✓ CI Verified',
  FAIL: '✗ CI Failed',
  SKIP: '⊘ Skipped',
};

const classList = [
  'text-[10px]',
  'font-mono',
  'px-1.5',
  'py-0.5',
  'rounded',
  'border',
  colors[status] || '',
  compact ? '' : 'ml-1',
]
  .filter(Boolean)
  .join(' ');
---

{
  runUrl ? (
    <a
      href={runUrl}
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex"
    >
      <span class={classList}>
        {labels[status] || status}
      </span>
    </a>
  ) : (
    <span class={classList}>
      {labels[status] || status}
    </span>
  )
}
